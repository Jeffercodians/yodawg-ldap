#!/usr/bin/env bash

set -ueo pipefail

include "<docker>"

# @cmd
up() {
	docker::compose up -d
}

# @cmd
down() {
	docker::compose down
}

# @cmd
status() {
	docker::compose ps
}

# @cmd
logs() {
	docker::compose logs
}

# @cmd
list_users() {
	:
}

# @cmd
# @arg name
add_user() {
	:
}

# @cmd
# @arg name
remove_user() {
	:
}

eval "$(argc --argc-eval "${0}" "${@}")"

